services:
  - type: web
    name: hackathon-team-finder
    env: node
    buildCommand: npm run install-all
    startCommand: node index.js
    envVars:
      - key: NODE_VERSION
        value: 20.11.0
